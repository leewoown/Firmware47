# SW 변경내역 — F28069 Pack BMS (V0.23)

- **제품/시스템**: 현대 10kW 수소연료전지(FC) Pack BMS · F28069 (80V, 22S1P)
- **버전**: V0.22 → **V0.23** (Product_Type=0 시험 / Product_Version=23)
- **작성일**: 2026-09-10
- **작성자**: leewoowon (Note1)
- **기준 커밋**: 64c69f3 (V0.22) → **본 버전 커밋 6fb18eb (V0.23, 2026-09-10)**
- **변경 근거 문서**: `10kWFCU_BAP2ndProtocol(20260910)R14.md`
- **검증상태**: BATIC 통신 정상 상태에서 NVRAM 지연 초기화·0x608 R14 반영. isoSPI 판정은 비활성(원복 대기).

---

## 1. 요약

V0.22(BATIC SPI 통신 복구) 이후, **부팅 전 NVRAM 접근이 BATIC 를 손상시키는 문제**를 구조적으로 정리했다.
`NVR_Init()` 을 부팅 시퀀스에서 빼고 **LTC6804 초기화·셀전압 취득이 끝난 뒤 1회 지연 호출**로 옮겼으며,
간헐 오검출이 있던 **PackISO_ERR 판정을 일시 비활성**하고, **0x608 카운터를 규약 R14** 로 정합했다.

---

## 2. NVRAM (핵심)

| 항목 | 이전 | V0.23 |
|---|---|---|
| `NVR_Init()` 호출 시점 | 부팅 시퀀스(MemCopy 뒤, while(1) 진입 전) | **INIT 완료·LTC6804 초기화 후 지연 호출** (`NvrInitDone` 1회) |
| `NVRAM_SelfTest()` | (V0.22에서 제거) | 제거 유지 |
| `NvrOk` 실패값 | `0` (초기값과 동일) | **`2`** — 0=미실행 / 1=정상 / 2=실패 (0x608 byte5, 규약 R15) |

- **근거**: while(1) 진입 전(= LTC6804 초기화 전)에 SPI 를 쓰면 BATIC 이 비정상이 되어 셀전압을 전혀 못 읽는다(실측: 셀전압 memset 초기값 3.25V 고정, IsoSpiErr 계속 증가). 런타임 NVRAM 접근(AZoneSave/Read)은 정상이므로, 셀전압 취득 완료 시점에 쓰기보호 해제만 1회 수행.
- **런타임 SOC NVR 저장/복원은 정상 동작** (AZoneSave/AZoneRead 활성).

---

## 3. isoSPI / BATIC

- **PackISO_ERR 판정 일시 비활성** (main.c 주석) : 간헐 펄스(CAN 관측)로 통신 규명 후 원복 예정.
- **ltc_state 단독 조건 제거** : 단일 실패에 즉시 트립하지 않도록 정리(원복 시 디바운스 설계).

---

## 4. 통신 — 0x608 R14 정합

| 배치 | 이전(R13) | V0.23(R14) |
|---|---|---|
| byte0~5 | 16bit×3 (CANRx/CT_Rx/VCU_Rx) | 8bit×4 (CANRx/CT_Rx/VCU_Rx/SpiTimeout) + 예약 16bit |
| byte6~7 | — | 16bit |

- 진단 카운터 `NvrOk` 0/1/2 구분은 규약 R15 로 이어짐.

---

## 5. NVR SOC 복원

- `LastSOC == 0` 을 **무효 조건에 추가** (`< 0` → `<= 0`) : 저장된 적 없는(0) SOC 를 유효로 오인해 0% 복원하는 것 방지.

---

## 6. 변경 파일

| 파일 | 변경 |
|---|---|
| `SysSoure/main.c` | NVR_Init 지연 호출, PackISO_ERR/ltc_state 판정 정리, 0x608 R14, LastSOC 무효조건 |
| `SysSoure/NVRAM.c` | NvrOk 실패값 0 → 2 |
| `SysInclude/parameter.h` | Product_Version 22 → 23 |

- 커밋 `6fb18eb` (SW 소스 3개만).

---

## 7. 잔여 / 후속 조치 (→ V0.24 이후)

| # | 항목 | 버전 |
|---:|---|---|
| 1 | NVRAM 무응답(부팅 전 접근) **근본 원인 규명** | V0.24+ |
| 2 | PackISO_ERR 판정 **디바운스 설계 후 원복** | V0.24+ |
| 3 | **SOC 알고리즘 수정** (계획분) | **V0.24** |
| 4 | CCS 클린 빌드 검증 | 사용자 |
| 5 | 0x608 R14 / NvrOk R15 상위장치(VCU) 정합 확인 | 시험 |
