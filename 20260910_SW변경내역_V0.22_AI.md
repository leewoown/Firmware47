# SW 변경내역 — F28069 Pack BMS (V0.22)

- **제품/시스템**: 현대 10kW 수소연료전지(FC) Pack BMS · F28069 (80V, 22S1P)
- **버전**: V0.20 → **V0.22** (Product_Type=0 시험 / Product_Version=22)
- **작성일**: 2026-09-10
- **작성자**: leewoowon (Note1)
- **기준 커밋**: 7be1092 (V0.20) → **본 버전 커밋 64c69f3 (V0.22, 2026-09-10)**
- **변경 근거 문서**: `10kWFCU_BAP2ndProtocol(20260910)R14.md`, `SOC_FailR3.md`
- **검증상태**: BATIC SPI 통신 복구 **실기 확인 완료**. 나머지 검증중.

> V0.21 은 별도 커밋 없이 진행한 baseline 으로, 이 구간에서 **BATIC SPI 통신 불능**이 발생했다. 원인 복구분을 V0.22 로 커밋한다.

---

## 1. 요약

V0.21 에서 발생한 **BATIC(LTC6804 isoSPI) 통신 불능**을 복구했다. 원인은 4가지가 겹쳐 있었고(2절),
실기에서 통신 정상화를 확인했다. 이와 함께 **isoSPI 통신 에러 판정(PackISO_ERR)** 을 신설하고,
0x608 카운터를 **규약 R14** 로 정합, 260902 에 처리한 **CAN 수동 SOC 결함 4건**과 **SOC 알고리즘 보완**을 함께 편입했다.

---

## 2. BATIC SPI 통신 불능 — 원인·복구 (핵심)

| # | 원인 | 복구 | 파일 |
|---:|---|---|---|
| 1 | `DebugBoardMode=1` (모사장치 CAN 모드) → 실기 `SlaveVoltagHandler` 컴파일 제외, 메일박스 모사 ID | **1 → 0** (실기 isoSPI 복귀) | parameter.h |
| 2 | `BATSPIEnable_low` CS-Low 설정 누락(시험 중 제거) → NVRAM 이 `SPIBRR=50` 으로 바꾼 뒤 미복원 | CS-Low 직전 **SPIBRR=60/CLKPOLARITY=1 재기입 원복** | BAT_LTC6802.c |
| 3 | `SPI_Write/Read` 무한 대기 → 부팅 중 NVRAM 접근에서 멈춰 `main while(1)` 미진입 | **타임아웃(C_SpiWaitTimeout=60000) + 타임아웃 시 `SPISWRESET` 0→1 복구** | F2806x_Spi.c |
| 4 | `NVR_Init/NVRAM_SelfTest` 재활성 시 **NVRAM 무응답**으로 전 호출 타임아웃(`SpiTimeout=10` 관측) → BATIC 까지 지연 | **두 호출 재차 제거** (BATIC 확보 우선, 원인 별도 규명) | main.c |

- 보조: SPI CS 초기 레벨 **High(비활성)** 지정 (부팅 초기 CS 상태 확정).
- 타임아웃 60000 회 ≈ 수 ms ≫ 328kHz 1바이트(24µs) → 정상 전송은 트립하지 않음.

---

## 3. isoSPI 통신 에러 판정 신설 (No.35 PackISO_ERR)

```c
if((ltc_state == 0) || (ltc_error_count > C_IsoSpiErrLimit))   // 200 연속 실패
    SysRegs.BAT80VFaultReg.bit.PackISO_ERR = 1;                // isoSPI 통신 에러 Fault
else
    SysRegs.BAT80VFaultReg.bit.PackISO_ERR = 0;                // 통신 정상이면 해제
```

- `ltc_error_count` 는 `LTC6804_read_cmd` PEC 일치 시 0, 불일치 시 +1 (연속 실패 카운터).
- 통신 성공 시 `SlaveReg.ErrorCount` **리셋** 추가 — 종전엔 증가만 하여 복구 후에도 0x608 Slave_Err 이 계속 에러로 보였다.
- 진단 카운터(`SpiTimeoutCount` 등) **0 리셋 → 200 포화** (8bit 송신·이력 유지).

---

## 4. 0x608 배치 R14 정합

| 바이트 | 이전(R13) | V0.22(R14) |
|---|---|---|
| byte0~5 | 16bit×3 (CANRx / CT_Rx / VCU_Rx) | **8bit×4** : CANRxCount / CT_RxCount / VCU_RxCount / **SpiTimeout(신규)** + 예약 16bit |
| byte6~7 | — | 8bit×2 |

---

## 5. CAN 수동 SOC 설정 결함 4건 수정 (260902)

- raw **0.1% → %** 환산 누락 정정
- SOC **상·하한 clamp** 추가
- **기준 SOC 교체** (명령값을 SysSOC 기준으로)
- 명령값 안착 위한 **적산 리셋**, **1회성 트리거 클리어 / 재진입 방지**

> 0x400 에지검출 방식은 미반영 결정(현행 유지).

---

## 6. SOC 알고리즘 보완 (260902, BATAlgorithm)

- **R-7** 무부하 지속 판정 `1800s → 600s` 완화
- **F-5**(휴지 중 전압변화 판정) main.c → `CalSocHandle` 이관
- 적산 **windup 차단**, 램프 결과 **범위 제한** 추가
- 부팅 초기화 **이중화 정리** + NVR **F-5 입력 전달**(저장 셀전압[mV])

---

## 7. 파라미터 (parameter.h)

| 매크로 | 값 | 용도 |
|---|---:|---|
| `C_SpiWaitTimeout` | 60000U | SPI 대기 루프 타임아웃 상한 |
| `C_SpiTimeoutMax` | 200U | `SpiTimeoutCount` 포화 상한(8bit 송신) |
| `C_IsoSpiErrLimit` | 200 | `PackISO_ERR` 판정 임계 |
| `Product_SysCellVauleS` / `P` | 22 / 1 | 셀 구성 22S1P |
| `Product_Voltage` / `Product_Capacity` | 809 / 564 | 정격 80.9V / 56.4Ah |
| `DebugBoardMode` | 0 | 실기 isoSPI |
| `Product_Version` | 22 | Type.Version = 0.22 |

---

## 8. 변경 파일

| 파일 | 변경 |
|---|---|
| `C2806XSrc/F2806x_Spi.c` | SPI 대기 타임아웃 + SW reset 복구, 진단 카운터 |
| `SysSoure/BAT_LTC6802.c` | BATSPIEnable_low CS-Low 설정 원복, ErrorCount 성공 시 리셋 |
| `SysSoure/main.c` | NVR 호출 재차 제거, SPI CS 초기화, PackISO_ERR 판정, 0x608 R14, 수동 SOC 4건, 초기화 정리 |
| `SysSoure/BATAlgorithm.c` | SOC R-7/F-5/windup/램프 보완 |
| `SysInclude/BATAlgorithm.h` | SOC 관련 구조체 필드 |
| `SysInclude/parameter.h` | 신규 매크로 3종·셀구성·정격·DebugBoardMode 0·Product_Version 22 |

- 커밋 `64c69f3` (SW 소스 6개만, IDE 설정·문서·`_to_delete/` 제외).

---

## 9. 잔여 / 후속 조치

| # | 항목 | 담당 |
|---:|---|---|
| 1 | **NVRAM 무응답 원인 규명** 후 `NVR_Init/NVRAM_SelfTest` 재도입 — 현재 **SOC NVR 저장/복원 비활성** | 설계 |
| 2 | CCS 클린 빌드 검증 | 사용자 |
| 3 | `PackISO_ERR` 임계 200·해제 동작 실차 검증 | 시험 |
| 4 | 0x608 R14 배치 상위장치(VCU) 정합 확인 | 시험 |
| 5 | R3 서문 「이용온도 밖 0A」 정책 적용 여부(V0.20 보류분) | 설계 |
