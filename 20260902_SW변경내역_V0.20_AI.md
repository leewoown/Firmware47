# SW 변경내역 — F28069 Pack BMS (V0.20)

- **제품/시스템**: 현대 10kW 수소연료전지(FC) Pack BMS · F28069 (80V, 22S)
- **버전**: V0.19 → **V0.20** (Product_Type=0 시험 / Product_Version=20)
- **작성일**: 2026-09-02
- **작성자**: leewoowon (Note1)
- **기준 커밋**: 3a9c8cc (직전) → **본 버전 커밋 7be1092 (V0.20, 2026-09-02)**
- **변경 근거 문서**: `20260831_P56_Safety_Current_Limit_R3.md`
- **검증상태**: 검증중 (CCS 빌드 확인 필요)

---

## 1. 요약

P56 안전 전류제한 함수를 **구조 리팩터**했다. 판정·산출 로직은 그대로 두고,
① 함수를 `DSP28x_Project.c` → `BATAlgorithm.c` 로 이동하고 이름을 `CalP56CurrentLimit` 으로 통일,
② 파라미터를 `SystemReg *s` → `SocReg *p` 로 바꿔 입출력을 SOC 레지스터(`Farasis56AhSocRegs`)로 이관했다.
전류한계 4개 표는 근거 문서 **R3** 과 396셀 전량 일치함을 자동 대조로 확인했다(값 변경 없음).
R3 서문의 「이용온도 밖 0A(사용 금지)」 정책은 **주석 보류**하여, 현재는 **표대로만 동작**한다.

---

## 2. 변경 상세

### 2-1. BATAlgorithm.c — 함수 이관·리네임·시그니처 변경

| 항목 | 이전 | V0.20 |
|---|---|---|
| 함수 위치 | `DSP28x_Project.c` | **`BATAlgorithm.c` (`#if FarasisP56Ah` 내)** |
| 함수명 | `PackCurrentLimit` → `P56PackCurrentLimit` | **`CalP56CurrentLimit`** |
| 파라미터 | `SystemReg *s` | **`SocReg *p`** (본체 `s->` → `p->` 일괄) |
| 동반 이동 | — | `PackLimitLookupF`, 전류한계 4표, 온도/SOC 축, `C_PackLimit*` 매크로 |

- extern 선언은 `#if FarasisP56Ah` 블록 상단에 배치(`CellP56AhSocInit` 옆) — P56 전용 선언 관례와 일치.

### 2-2. BATAlgorithm.h — SocReg 확장

`SocReg` 에 P56 전류제한 입출력 **9 멤버** 추가.

| 구분 | 멤버 |
|---|---|
| 입력 4 | `Bat80VCellAgvTemperatureF`, `Bat80VSOCF`, `Bat80VCurrentF`, `Bat80VCurrentAsbF` |
| 출력 5 | `Bat80VDisCHAPeakFCurrF`, `Bat80VCHAPeakFCurrF`, `Bat80VDisCHAContintyCurrF`, `Bat80VCHAContintyCurrF`, `Bat80VUnbalCurrentF` |

### 2-3. DSP28x_Project.c — 구 정의 제거·초기값 통일

- 구 `PackCurrentLimit`/표/헬퍼 제거(BATAlgorithm.c 로 이동), 구 extern 폐지(주석).
- `SysVarINIT` 전류한계 초기값 **0 → 5.0F** (한계 4종), `Bat80VUnbalCurrentF` 0.
  - CAN 0x604 초기 표시 **충전 +5.0A / 방전 -5.0A** 기준으로 `CalSocRegsInit` 과 통일.

### 2-4. main.c — caller 입출력 배선

`#if FarasisP56Ah` 가드 내에서:

```c
// 입력 4 : SysRegs -> SocRegs
Farasis56AhSocRegs.Bat80VCellAgvTemperatureF = SysRegs.Bat80VCellAgvTemperatureF;
Farasis56AhSocRegs.Bat80VSOCF                = SysRegs.Bat80VSOCF;
Farasis56AhSocRegs.Bat80VCurrentF            = SysRegs.Bat80VCurrentF;
Farasis56AhSocRegs.Bat80VCurrentAsbF         = SysRegs.Bat80VCurrentAsbF;

CalP56CurrentLimit(&Farasis56AhSocRegs);

// 출력 5 : SocRegs -> SysRegs (하류 0x604 송신·경고/보호 판정용)
SysRegs.Bat80VDisCHAPeakFCurrF    = Farasis56AhSocRegs.Bat80VDisCHAPeakFCurrF;
SysRegs.Bat80VCHAPeakFCurrF       = Farasis56AhSocRegs.Bat80VCHAPeakFCurrF;
SysRegs.Bat80VDisCHAContintyCurrF = Farasis56AhSocRegs.Bat80VDisCHAContintyCurrF;
SysRegs.Bat80VCHAContintyCurrF    = Farasis56AhSocRegs.Bat80VCHAContintyCurrF;
SysRegs.Bat80VUnbalCurrentF       = Farasis56AhSocRegs.Bat80VUnbalCurrentF;
```

- 함수 선언 `void CalP56CurrentLimit(SocReg *p);` (main.c 상단).

### 2-5. 버전

```c
//#define     Product_Version                    19   // (0.19 커밋 완료분)
#define     Product_Version                      20   // TODO : [변경] 260902_Note1, 0.20 Product_Version 19->20 (P56 전류제한 이관·SocReg화)
```

---

## 3. 전류한계 표 검증 (근거 문서 R3 대조)

자동 대조 결과 **4표 × 9온도 × 11 SOC = 396셀 전량 일치** (불일치 0). 방전은 크기(양수) 기준 비교.

| 펌웨어 표 | R3 표 | 결과 |
|---|---|:--:|
| `PackDisChaPeakLimitF` | 5sec Discharge | 99/99 일치 |
| `PackChaPeakLimitF` | 5sec Charge | 99/99 일치 |
| `PackDisChaContLimitF` | Continuous Discharge | 99/99 일치 |
| `PackChaContLimitF` | Continuous Charge (R3 Note6 상향 포함) | 99/99 일치 |

- 연속 충전표는 V0.18 에서 R3 Note6(10~50℃, SOC 50~70%=1.3C 72.8A) 이미 반영, 구 R1/R2 값 주석 보존.

---

## 4. 부호 규약 (충전 + / 방전 −)

| 계층 | 방전 부호 | 근거 |
|---|---|---|
| 표/내부저장 | 양수 크기 | 산출·판정 단순화 |
| 초과분 판정(`Bat80VUnbalCurrentF`) | 크기끼리 비교(>0=초과) | 경고 No.8 / 보호 No.23 |
| **CAN 송신(0x604)** | **음수** (`-1.0F` 반전, main.c) | **충전+/방전− 규약 충족** |

- 이중부호 아님(factor 0.1 양수 1개, 방전은 송신 raw 자체가 음수).

---

## 5. R3 서문 「이용온도 밖 0A」 — 보류

- R3 서문/Note2: 이용온도(−10~55℃) 밖(취소선 −30/−20/60℃)은 「BMS 적용 시 0A(사용 금지)」 권고.
- 사용자 결정으로 **미적용**. 표대로 동작(취소선 행도 참고값 보간에 사용).
- 구현 흔적 보존: `CalP56CurrentLimit` 출력단 0A 강제 가드 + `C_PackLimitUsableTempMin/MaxF` 매크로 **주석 보류**(되살릴 때 해제).
- 261220 게이팅은 **폴트(차단) 전용**(No.23 등)이며 본 0A 처리는 폴트가 아니므로 게이팅 대상 아님.

---

## 6. 구현 검증 결과 (SW 오류 확인)

| 검증 항목 | 결과 |
|---|:--:|
| 함수명 정합(활성 `CalP56CurrentLimit` 1벌, 구명 참조 0) | OK |
| SocReg 9 멤버 정의 | OK |
| 함수 접근 멤버 8종 모두 SocReg 소속·오타 없음 | OK |
| 초기화(CalSocRegsInit 9종 / SysVarINIT 동일 기준) | OK |
| caller 입력4 + 출력5 복사 완비 | OK |
| 0A 가드·매크로 주석 → 활성 미참조·미정의 참조 없음 | OK |
| 전처리기 균형(BATAlgorithm 6/6, DSP28x 2/2, main 18/18) | OK |

→ 컴파일/링크/런타임 관점 이상 없음.

---

## 7. 변경 파일

| 파일 | 변경 |
|---|---|
| `SysSoure/BATAlgorithm.c` | 함수 이동·리네임·`SocReg *p`, 표/헬퍼/매크로 이관, 0A 가드 주석 |
| `SysInclude/BATAlgorithm.h` | SocReg 9 멤버 + extern |
| `SysSoure/DSP28x_Project.c` | 구 정의 제거, SysVarINIT 초기값 5.0F, 구 extern 폐지 |
| `SysSoure/main.c` | caller 입출력 복사·호출·선언 |
| `SysInclude/parameter.h` | `Product_Version` 19 → 20 |

- 커밋 `7be1092` (SW 소스 5개만, IDE 설정·`_to_delete/` 제외).

---

## 8. 후속 조치

| # | 항목 | 담당 |
|---:|---|---|
| 1 | CCS 클린 빌드 검증 | 사용자 |
| 2 | 실차/HIL 에서 온도·SOC별 4종 전류한계 출력값 및 0x604 송신 부호 확인 | 시험 |
| 3 | R3 서문 0A 정책 적용 여부 최종 결정(보류 → 적용 시 가드·매크로 주석 해제) | 설계 |
| 4 | CAN DB 0x604 Min/Max(±10) → 0 포함 완화 검토(소전류·0A 값 대응) | 설계 |
| 5 | 배터리 제조사/한국전력연구원 5sec Discharge 한계 재평가(R3 미검증 항목) | 설계 |
