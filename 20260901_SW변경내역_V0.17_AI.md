# SW 변경내역 — F28069 Pack BMS (V0.17)

- **제품/시스템**: 현대 10kW 수소연료전지(FC) Pack BMS · F28069 (80V, 22S)
- **버전**: V0.16 → **V0.17** (Product_Type=0 시험 / Product_Version=17)
- **작성일**: 2026-09-01
- **작성자**: leewoowon (Note1)
- **기준 커밋**: 4f1aaaa (V0.16, 2026-09-01)
- **검증상태**: 검증중

---

## 1. 요약
통신규약 **R12→R13** 반영. CAN 페이로드 재편(BATIC 내부온도, 셀 Multiplex, 카운터 16bit)에 맞춰 레지스터·송신부 정합. 펌웨어 구조체 BIT ↔ R13 규약 일치 검증 완료.

## 2. 변경 상세 (R13 반영)

### 2-1. 0x602 (BSA3)
- **BMS_BATIC_Temp @ bit48** 신설 (D3, signed 0.1℃) — `CANReg.BATIC_Temp`(int16) + `SystemReg.Bat80VBATICTemperatureF`(float) 레지스터 신설·초기화·값반영. 소스 `Slave1Regs.BatICTempsF`.
  - 송신: `CANATX(0x602, ..., BAT80VAh, BATIC_Temp)` (기존 D3 StateReg → BATIC_Temp 교체)
- **BSA_Neg_Rly = 1 고정** — N 릴레이 물리 제거(배터리(−) 직결)이므로 항상 1(Close) 보고. (`NRlyOUT = 1`)

### 2-2. 0x609 (BSA10) — 셀 Multiplex
- 셀 전압/온도 Mux: 전압 Mux 0~7, **셀온도 Mux 8~15** 정렬 (`CANATX(0x609, 8+CellTempsTxNum/3, ...)`), 셀번호=(Mux-8)*3.
- 기존 0x609 BATIC_Temp 송신 라인은 0x602로 이동에 따라 주석 처리.

### 2-3. 0x608 (BSA9) — 카운터 16bit
- 코드는 이미 16bit×3 + 8bit×2 배치(CANTxA/B/C=MailBox 카운트 풀값, CANTxD=ComBine(Slave2,Slave1))로 **R13과 일치** → 로직 변경 없음.
- 주석만 R12(8bit×5) → **R13(16bit×3+8bit×2, byte0~1/2~3/4~5)** 로 정정.

### 2-4. 버전
- Product_Version 16 → **17** (VER 0.17).

## 3. R13 정합 검증 결과 (구조체 BIT)
- **0x603 Warn(bit0~14) 15비트** — `SystemAlarm_BIT` ↔ R13 `BSA_Wn_*` 전부 일치.
- **0x603 Protect(bit16~36) 21비트** — `SystemProtect_BIT` int0~20 ↔ R13 `BSA_Prtct_*` 전부 일치.
- **0x603 송신은 `BAT80VFaultReg` 사용이 의도** (ProtectReg와 동일 레이아웃, 변경 안 함).

## 4. 미반영 / 검증 대기 (TODO)
- **BATIC_Temp 값반영 의존성**: `Slave1Regs.BatICTempsF = Slave1Temps;`(main.c 799 부근) 주석 처리 상태 → BATIC_Temp가 stale/0일 수 있음. 소스 확정(직접 `Slave1Temps` 사용 등) 필요.
- NVRAM 자체진단(260831) 미반영(ITRAP 소지, 별도 세션 되돌림).
- ISOSPI 통신오류 판정 로직 미구현(카운트 매크로만).
- FaultDelay=0 동작 검증용 — 검증 후 정상 유지시간 복원 필요.

## 5. 변경 파일 (커밋 예정)
`main.c`, `DSP28x_Project.c`, `DSP28x_Project.h`, `parameter.h` (+ SW변경내역 md)

## 6. 참조 문서
통신규약서 CAN R12/R13(20260901)
