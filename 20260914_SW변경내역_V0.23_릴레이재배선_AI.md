# SW 변경내역 — F28069 Pack BMS (V0.23 · 릴레이 재배선)

- **제품/시스템**: 현대 10kW 수소연료전지(FC) Pack BMS · F28069 (80V, 22S1P)
- **버전**: V0.23 유지 (Product_Type=0 시험 / Product_Version=23) — 0.23 두 번째 커밋
- **작성일**: 2026-09-14
- **작성자**: leewoowon (Note1)
- **기준 커밋**: 6fb18eb (V0.23 NVRAM/isoSPI) → **본 커밋 306a401 (V0.23 릴레이, 2026-09-14)**
- **검증상태**: HW 재배선 반영. 릴레이 시퀀스 실기 검증 필요.

> 0.23 은 두 커밋으로 구성: `6fb18eb`(NVRAM 지연초기화·isoSPI·0x608 R14) + `306a401`(릴레이 HW 재배선). 버전 매크로는 23 유지(사용자 결정).

---

## 1. 요약

**HW/PCB 재배선**에 따라 릴레이 제어 시퀀스를 변경했다. N 릴레이가 물리 제거되어 BATTERY N 이 직결되고,
기존 GPIO 두 개가 각각 **주(P) 릴레이 / 초중(프리차지) 릴레이 코일**로 재배정되었다.
`ProtectRelayWakeUpHandle`의 ON/OFF 시퀀스를 새 배선에 맞게 정리했고, 옛 시퀀스는 주석 보존했다.
아울러 CCS(MS949) 저장으로 깨졌던 `ProtectRelay.c` 한글 주석을 **UTF-8 정상 복구**했다.

---

## 2. HW/PCB 재배선 (근거)

| 신호 | 이전 | V0.23(현재) |
|------|------|-------------|
| BATTERY N | N 릴레이 경유 | **직접 연결** (N 릴레이 물리 제거) |
| `N_RELAY` (GPIO06) | N 릴레이 코일 | **P(주) 릴레이 코일** |
| `P_RELAY` (GPIO22) | P(주) 릴레이 코일 | **초중(프리차지) 릴레이 코일** |

- 매크로 명칭(`NRlyOn`=GPIO06, `PRlyOn`=GPIO22)은 유지, **물리 대상만 재배정**됨.
- 참고: N 직결이므로 `NRelayDO` 상태비트는 항상 1 보고.

---

## 3. WakeUp ON 시퀀스 (ProtectRelayWakeUpHandle)

**새 시퀀스** — 프리차지 후 주접점 투입:
```c
NRelayDO=1;                 // N 직결 상태 보고
PRlyOn;  PreRelayDO=1;      // GPIO22 = 초중(프리차지) 릴레이 ON
delay_ms(200);
NRlyOn;  PRelayDO=1;        // GPIO06 = P(주) 릴레이 ON
delay_ms(100);
PRlyOff; PreRelayDO=0;      // 초중(프리차지) 릴레이 OFF
WakeUpState=1;
```
- 옛 배선 시퀀스(`N→CHA→P`, delay 30/200/100)는 `/* */` 주석 보존(검증 후 제거).

---

## 4. WakeUp OFF 시퀀스

**변경**: `delay_ms(250)` 제거, `PRelayDO=0` 을 `NRelayDO=1` 앞으로 이동.
```c
NRlyOff;                    // GPIO06 = P(주) 릴레이 OFF
p->State.bit.PRelayDO=0;
p->State.bit.NRelayDO=1;    // N 직결(항상 1)
p->State.bit.WakeUpState=0;
```
- 옛 시퀀스(`NRlyOff→NRelayDO=1→delay250→PRelayDO=0`)는 `//` 주석 보존.

---

## 5. 인코딩 복구

- `ProtectRelay.c` 가 CCS(MS949) 저장으로 UTF-8/CP949 혼합 손상 → **정상 UTF-8 복구**(한글 주석 정상).
- **재발 방지**: CCS 프로젝트 Text file encoding 을 **UTF-8** 로 설정(Project → Properties → Resource). 커밋 원본이 UTF-8 이므로 통일 필요.

---

## 6. 변경 파일

| 파일 | 변경 |
|------|------|
| `SysSoure/ProtectRelay.c` | WakeUp ON/OFF 시퀀스 재배선 + 인코딩 UTF-8 복구 |

- 커밋 `306a401` (SW 소스 1개). Product_Version 미변경(23).

---

## 7. 후속 조치

| # | 항목 | 담당 |
|---:|---|---|
| 1 | **릴레이 시퀀스 실기 검증** — 프리차지 200ms/주접점 100ms 타이밍, OFF delay 제거 영향 | 시험 |
| 2 | 옛 시퀀스 주석 블록 제거(검증 완료 후) | 설계 |
| 3 | CCS Text file encoding = UTF-8 설정(재발 방지) | 사용자 |
| 4 | CCS Clean → Rebuild 후 빌드 확인 | 사용자 |
