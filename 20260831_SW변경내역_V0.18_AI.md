# SW 변경 내역 — Ver 0.18 (Product_Type 0 / Product_Version 18)

> **R1 / 2026-08-31** | 작성 이우원 재생본부장
> 대상 펌웨어 : `D:\47 HDFC10kWNCM_PackBMSR1\F28069PackBMS`
> 이전 버전 : 0.17 (Product_Version 17)
> 변경 근거 문서 : `20260831_P56_Safety_Current_Limit_R3.md` / `SOC_FailR1.md` / `10KWFCUBAS_PrtotectSet(260827)R6.md`

---

## 1. 변경 요약

| # | 구분 | 항목 | 근거 | 반영 |
|---|---|---|---|---|
| 1 | 보호 | `Bsa_WrnUnbalPwr` / `Bsa_FltUnbalPwr` 임시 기능 차단 | 운용 요청 | ✅ |
| 2 | 보호 | `C_PackTempUN_Fault` 변경 이력 주석 보완 (값은 -25.0 유지) | 이력 관리 | ✅ |
| 3 | 버전 | `Product_Version` 17 → 18 | 차수 갱신 | ✅ |
| 4 | 전류한계 | 연속 충전 한계표 R3 반영 (10~50 ℃) | Safety Current Limit R3 | ✅ |
| 5 | NVRAM | SPI baud rate 미복원 수정 (F-4) | SOC_FailR1 | ✅ |
| 6 | NVRAM | 쓰기보호 해제 호출 + 해제 결과 진단 (F-1) | SOC_FailR1 | ✅ |
| 7 | NVRAM | 읽기 유효성 검사 복구 (F-2) | SOC_FailR1 | ✅ |
| 8 | NVRAM | 부팅 1회 자체 진단 신규 (F-3) | SOC_FailR1 | ✅ |
| 9 | SOC | 재기동 시 휴지 전압 판정 → NVR 우선 (F-5) | SOC_FailR1 | ✅ |
| 10 | SOC | 운전 중 OCV 완만 보정 (F-6) | SOC_FailR1 | ✅ |
| 11 | SOC | 적산분(SysAhF) 복원 (F-7) | SOC_FailR1 | ❌ **미반영** |

---

## 2. 보호 기능

### 2-1. `Bsa_WrnUnbalPwr` / `Bsa_FltUnbalPwr` 임시 차단

전류한계 초과 경고(No.8) 및 폴트(No.23) 판정을 임시로 막았다. 판정부는 삭제하지 않고 주석으로 보존했으며, 잔류 비트가 남지 않도록 관련 레지스터를 매 주기 0으로 강제한다.

| 위치 | 변경 |
|---|---|
| `SysSoure/DSP28x_Project.c:596~614` | Alarm 판정부 주석 처리 → `Bat80VAlarmCont[7]=0`, `BAT80VAlarmReg.bit.PackUnPWR_BL=0` |
| `SysSoure/DSP28x_Project.c:874~898` | Fault 판정부 주석 처리 → `BatFalutCont[7]=0`, `BAT80VFaulBuftReg`/`BAT80VFaultReg` 의 `PackUnPWR_BL=0` |

- Fault 쪽은 기존 else 절에 없던 `BAT80VFaultReg.bit.PackUnPWR_BL = 0` 을 추가했다. 차단 전에 이미 세팅된 폴트 비트가 남는 것을 막기 위함이다.
- 파라미터 `C_PackUnbalPwr_WarnDelay`(10 s) / `C_PackUnbalPwr_FaultDelay`(15 s)는 손대지 않았다.
- `PackCurrentLimit()` 의 초과분 산출(`Bat80VUnbalCurrentF`)은 그대로 동작하므로 **CAN 보고값(0x604)은 정상**이며, 알람·폴트로만 나타나지 않는다.

> **복구 방법** : 두 블록의 `//` 를 되살리고 강제 클리어 라인(각 2줄 / 3줄)을 제거한다.

### 2-2. `C_PackTempUN_Fault` 이력 주석 보완

값 자체는 이전 작업에서 이미 -25.0 으로 반영되어 있었고, 이번에는 **변경 이력 형식만** 규칙에 맞게 정리했다.

| 위치 | 변경 |
|---|---|
| `SysInclude/parameter.h:404~410` | 비고 박스 추가 + 이전 값 `//#define C_PackTempUN_Fault -35.0` 주석 보존 |

- 변경 사유 : 셀 저온 폴트(-30.0 ℃) 보다 높게 두어 팩이 먼저 검출되도록 함. 경고(팩 -20 / 셀 -25), 과온(팩 52 / 셀 60)과 방향이 일치한다.

---

## 3. 전류 한계표 (Safety Current Limit R3)

R3 문서의 4개 표 중 **연속 충전 한계표만 변경**되었다. 5 sec 방전 / 5 sec 충전 / 연속 방전 3개 표는 R1과 값이 완전히 동일하여 손대지 않았다.

| 위치 | 변경 |
|---|---|
| `SysSoure/DSP28x_Project.c:1308~1310` | 근거 문서 R1 → R3 갱신 이력 |
| `SysSoure/DSP28x_Project.c:1384~1396` | `PackChaContLimitF` 의 10 / 25 / 40 / 50 ℃ 행 교체 (구 값 주석 보존) |

### 변경값 (단위 A)

| 온도 | SOC | 변경 전 | 변경 후 |
|---|---|---|---|
| 10 ℃ | 30, 40 | 39.2 | **72.8** |
| 10 ℃ | 50, 60, 70 | 28.0 | **72.8** |
| 25 ℃ | 50 | 56.0 | **72.8** |
| 25 ℃ | 60, 70 | 33.6 | **72.8** |
| 40 ℃ | 50 | 134.4 | **72.8** |
| 40 ℃ | 60 | 89.6 | **72.8** |
| 40 ℃ | 70 | 67.2 | **72.8** |
| 50 ℃ | 0 ~ 50 | 56.0 | **72.8** |
| 50 ℃ | 60, 70 | 33.6 | **72.8** |

- 근거 : R3 Note 6 — 10~50 ℃ 구간 SOC 50~70 % = 1.3 C(72.8 A) 적용, SOC 0~40 % 는 1.3 C 이상을 유지하여 SOC 증가에 따라 단조감소.
- **40 ℃ SOC 50~60 % 는 유일하게 하향**되는 지점이다(134.4 / 89.6 → 72.8). 1.3 C 일괄 적용의 결과이며 문서 의도와 일치한다.

---

## 4. NVRAM (SOC 재기동 점프 대책)

### 배경

`SOC_FailR1.md` 의 N1 실기 시험 결과, 재기동 시 NVR(MRAM `LastSOC`) 복원이 동작하지 않고 매번 OCV로 SOC를 재초기화하는 것이 확인되었다(적산 40.0 % → 재기동 49.8 % = OCV 계산값). 판정식상 `delta = 10.0 %p ≤ 20` 이므로 NVR이 채택되어야 하나, 실제로는 `NVRSocInitF < 0`(무효 마커) 경로를 탄 것으로 추정된다.

### 4-1. F-4 — SPI baud rate 미복원 수정

| 위치 | 변경 |
|---|---|
| `SysSoure/NVRAM.c:535~541` | `NVR_SPIWrite()` 종료 직전 `SpiaRegs.SPIBRR = 119` 복원 |
| `SysSoure/NVRAM.c:584` | `NVR_SPIRead()` 종료 직전 동일 처리 |

- NVRAM 접근 시 `SPIBRR = 50` 으로 바꾼 뒤 되돌리지 않아, 이후 LTC6804 통신이 잘못된 속도로 동작했다.
- **복원값 119 는 `InitSpi()` 의 실제 설정값**(`BAT_LTC6802.c:72`)이다. 원 보고서의 "60" 은 오기이며 그대로 적용하면 LTC6804 클럭이 약 2배가 된다.
- `NVR_CE_H` → `delay_us(1)` 이후에 변경하므로 전송 중 baud 변경은 발생하지 않는다.
- 조기 return 경로(`buf == NULL`)는 `SPIBRR` 변경 전이라 복원이 불필요하다.

> `SPICCR` 을 `SPISWRESET` 없이 변경하는 문제(F-4 후단)는 이번 범위에서 제외했다.

### 4-2. F-1 — 쓰기보호 해제 호출 + 해제 결과 진단

| 위치 | 변경 |
|---|---|
| `SysSoure/main.c:200~208` | `InitSpi()` 직후 `NVR_Init()` 호출 추가 |
| `SysSoure/NVRAM.c:432~445` | `NVR_UnLock()` 끝에 RDSR 재확인 → `NVRAllRegs.SRStatus` 기록 |
| `SysInclude/NVRAM.h:68~80` | `NVRAllReg` 에 `SRStatus` / `NvrOk` 진단 필드 추가 |
| `SysSoure/NVRAM.c:24` | `NVRAllRegs` extern 선언 |

`NVR_Init()` / `NVR_UnLock()` 은 정의만 있고 호출처가 0건이었다. MR25H40 의 상태 레지스터 BP1:BP0(bit3:2)는 비휘발성이라, 보호가 걸려 있으면 WRITE 가 전부 무시되어 `LastSOC` 가 갱신되지 않는다.

**`SRStatus` 판독 기준**

| 값 | 의미 |
|---|---|
| `0xFF` | 칩 무응답 (미실장 · CS(GPIO11) · 전원) |
| `0x00` | MISO Low 고정 (배선) |
| `(sr & 0x0C) != 0` | BP 잔존 → 쓰기보호 해제 실패 |
| 그 외 | BP=00 정상, WREN 직후 WEL(bit1)=1 |

> **주의** : `NVR_Lock()` 도 호출처가 0건이므로 펌웨어가 BP를 건 적은 없다. 칩이 출하 상태(BP=00)라면 `NVR_Init()` 없이도 WRITE 는 되었어야 한다. 즉 F-1 은 아직 **가설이며, `SRStatus` 실측이 원인 확정의 근거**가 된다.

### 4-3. F-2 — 읽기 유효성 검사 복구

| 위치 | 변경 |
|---|---|
| `SysSoure/NVRAM.c:307~318` | `MetaVersion != Product_Version` 시 무효 마커 처리 |

```c
if (p->MetaVersion != (Uint16)Product_Version)
{
    p->LastSOC   = (int16)-1;   /* 무효 마커 → main.c 가 OCV 폴백 */
    p->MetaCRC   = 0u;
    p->LastCellV = 0u;
    p->LastAh    = (int16)0;
}
```

- 기존 주석 블록의 복구값 `LastSOC = 300`(30 %)은 **가짜 유효값**이 되어 위험하므로 채택하지 않고, 무효 마커(-1)로만 표시해 상위에서 OCV 폴백하도록 했다. 원 주석 블록은 그대로 보존했다.

### 4-4. F-3 — 부팅 1회 자체 진단 신규

| 위치 | 변경 |
|---|---|
| `SysSoure/NVRAM.c:113~147` | `NVRAM_SelfTest()` 신규 |
| `SysSoure/main.c:208` | 부팅 1회 호출 |
| `SysInclude/NVRAM.h:190~191, 215` | `NVR_SELFTEST_ADDR` 매크로 + 함수 선언 |

기존 `NVRAM_StateTest()` 는 패턴 생성 for 본문 · 상태 전이 · 비교 루프가 모두 주석 처리되어 검증 기능이 없었고 호출처도 0건이었다(기존 함수는 삭제하지 않고 그대로 둠).

- write → read 왕복을 그 자리에서 완료하고 결과를 `NVRAllRegs.NvrOk`(1 = 일치)에 남긴다. 상태머신을 쓰지 않는다.
- 진단 주소를 `NVR_SELFTEST_ADDR (0x0000E0)` 로 분리했다. 기존 `TEST_ADDR (0x000100)` 은 `NVR_LOG_BASE_ADDR` 과 동일해 로그 첫 레코드를 덮어쓴다.

### 4-5. 메타 영역 직렬화 확장 (32 → 36 byte)

| 위치 | 변경 |
|---|---|
| `SysInclude/NVRAM.h:106~114` | `NVRZoneAReg` 에 `LastCellV`(Uint16, mV) / `LastAh`(int16, 0.1 Ah) 추가 |
| `SysInclude/NVRAM.h:190` | `NVR_META_BYTES (36U)` 매크로 신규 |
| `SysSoure/NVRAM.c:206~214, 224` | 저장 직렬화 `buf[32..35]`, 쓰기 길이 36 |
| `SysSoure/NVRAM.c:278~280, 195` | 읽기 역직렬화, 읽기 길이 36 |
| `SysSoure/main.c:844~845` | 저장 시점에 셀 평균전압 · 적산 Ah 기록 |

메타 영역은 256 byte 가 할당되어 있고 32 byte 만 사용 중이었으므로 여유 공간을 사용했다.

> **호환성** : 구버전(0.17)으로 저장된 MRAM 은 `buf[32..35]` 가 미기록 상태다. 4-3 의 `MetaVersion` 검사가 이를 걸러내며, `Product_Version` 을 18로 올린 것이 이 역할을 한다 — 버전 갱신 후 **첫 부팅만 OCV 로 초기화**되고 이후부터 정상 동작한다.

---

## 5. SOC 알고리즘

### 5-1. F-5 — 재기동 시 휴지 전압 판정 (NVR 우선)

| 위치 | 변경 |
|---|---|
| `SysSoure/main.c:479~498` | 부팅 SOC 결정부 최상단에 전압 비교 분기 추가 |
| `SysInclude/BATAlgorithm.h` | `C_SocRestCellVoltDiffmV (5.0F)` 상수, `SocReg` 에 `RestVoltDiffF` / `NvrAdopted` 추가 |

```
dV = |부팅 시 셀평균전압[mV] - NVR LastCellV[mV]|

dV ≤ 5 mV  AND  NVR 유효  AND  LastCellV ≠ 0   → NVR(LastSOC) 채택
그 외                                          → 기존 선형/비선형 delta 판정 경로
```

- 차단 직전과 부팅 시 전압이 같으면 그 사이 상태 변화가 없었다고 보고 NVR 값을 이어받는다. **휴지시간을 재지 않으므로 RTC 등 추가 HW 가 불필요**하다.
- 기존 판정 경로(선형/비선형 + delta > 20 %p)는 그대로 남아 있고, 위 조건을 만족하지 못할 때만 동작한다.

### 5-2. F-6 — 운전 중 OCV 완만 보정

| 위치 | 변경 |
|---|---|
| `SysSoure/BATAlgorithm.c:400~478` | `CalSocHandle()` 에 완만 보정 로직 추가 |
| `SysInclude/BATAlgorithm.h:94~110` | 진입 조건 · 보정 속도 상수 신규 |
| `SysInclude/BATAlgorithm.h` | `SocReg` 에 상태 변수 10종 추가 |
| `SysSoure/BATAlgorithm.c:319~340` | `CalSocRegsInit()` 초기화 |
| `SysSoure/main.c:903~908` | 온도 · 셀편차 · 시스템 상태를 매 주기 전달 |

기존에는 부팅 시 한 번 정한 `SysSocInitF` 를 끝까지 사용하여 초기 오차가 영구히 남았다(`CalMeth = 1` 고정).

**진입 조건 (전부 AND)**

| 조건 | 값 | 매크로 |
|---|---|---|
| 무부하 지속 | \|I\| < 0.5 A 가 1800 s | `C_SocOcvAdjRestCount 36000UL` |
| 전압 수렴 | \|ΔV(120 s)\| ≤ 2 mV | `C_SocOcvAdjVoltSettlemV 2.0F` |
| 셀 편차 | < 50 mV | `C_SocOcvAdjCellDivF 0.05F` |
| 온도 | 10 ~ 40 ℃ | `C_SocOcvAdjTempMinF / MaxF` |
| 시스템 상태 | **READY 만** | `System_STATE_READY` |

**동작**

| 항목 | 값 |
|---|---|
| 보정 속도 | 0.01 %p/s (`C_SocOcvAdjRateF 0.0005F` / 50 ms) |
| 데드밴드 | \|err\| ≤ 1.0 %p → 보정 안 함 |
| 정지밴드 | \|err\| ≤ 0.5 %p → 보정 종료 (히스테리시스) |
| 1회 상한 | 무부하 인터벌당 최대 5.0 %p, 초과 시 플래그 세우고 중단 |
| 대상 | `SysSocInitF` 만 이동. 적산분 `SysAhF` 는 건드리지 않음 |
| 상태 표시 | 보정 중 `SoCStateRegs.bit.CalMeth = 0`, 그 외 1 |

**상태 조건에 대한 판단 근거**

- `System_STATE_STANDBY` 는 부팅 중 `main.c:603` 에서 곧바로 READY 로 전이하므로 무부하 1800 s 를 채울 수 없다 → **조건에서 제외**.
- `System_STATE_READY` 는 상위 `RUNStatus`(Ifcu_BASRun) = 0 인 상태로, 컨택터가 개방된 실제 대기 상태다 → **유일한 진입 상태**.
- `System_STATE_PROTECTER` 는 **의도적으로 제외**했다. 폴트로 컨택터가 열린 채 오래 머물러도 SOC 기준을 움직이지 않는 편이 안전하다는 판단이다. PROTECTER 진입 시 `RestTimeCount` · `OcvAdjUsedF` · `OcvAdjActive` 가 모두 초기화되고, 폴트 해제 후 READY 복귀 시 1800 s 를 처음부터 다시 센다.

> 보정 블록은 `#if FarasisP56Ah` 로 감쌌다. `OCVtoSOC_P56Ah()` 가 해당 조건부 안에만 정의되어 있어 다른 셀 설정에서 링크 에러가 나지 않도록 한 것이다.

### 5-3. F-7 — 적산분 복원 : **미반영 (현행 유지)**

원 보고서 B-3 은 NVR 채택 시 `SysAhF = LastAh / 10.0F` 로 복원하라고 하나, **그대로 적용하면 적산분이 두 번 더해진다.**

```c
/* 저장 시 (main.c:838) */
NVRZoneAWRRegs.LastSOC = (int16)(SysRegs.Bat80VSOCF * 10.0F);
/*   Bat80VSOCF = SysSOCF = SysSocInitF + SysAhF x C_SocNorm x 100
     → LastSOC 에는 적산분이 이미 포함되어 있다 */

/* 부팅 시 */
SysSocInitF = NVRSocInitF;                              /* = LastSOC/10, 적산분 포함 */
SysSOCF     = SysSocInitF + SysAhF x C_SocNorm x 100;   /* 여기서 또 더해짐 */
```

- 보고서가 지적한 "1.6 Ah = 3.6 %p 단차" 는 폐기된 누적 Ah 가 아니라 **마지막 저장 시점과 차단 시점 사이의 미저장 적산분**이다. 저장 주기 단축 또는 차단 직전 강제 저장으로 접근해야 하는 별개 사안이다.
- 따라서 `main.c:543~553` 의 `SysAhF = 0.0F` 는 근거 주석만 달고 **현행 유지**했다.
- `LastAh` 는 필드 · 직렬화까지만 반영하여 **이력 확인용으로 저장**한다. 복원 로직이 필요해지면 저장 구조는 이미 준비되어 있다.

---

## 6. 미반영 / 이월 항목

| 항목 | 사유 |
|---|---|
| F-4 후단 : `SPICCR` 을 `SPISWRESET` 없이 변경 | 이번 범위 제외 (TI 권장 절차 위배는 확인됨) |
| F-7 : 적산분 복원 | 위 5-3 근거로 미반영 |
| A-6 : 진단값 CAN 0x608 송출 | 미착수 |
| B-2 상세 튜닝 (슬루율 · 밴드 실차 검증) | 실차 데이터 확보 후 |
| `NVRAM_NVRSocInit` 스케일 불일치 | `main.c:843~844` 에서 `LastSOC`(×10)와 `SysSocInitF`(%)에 같은 값 대입. 별도 수정 필요 |
| `NVR_SPIWrite` / `NVR_SPIRead` 의 조용한 실패 | `if(SPI_Sema) return 0U;` 로 실패를 알리지 않음. LTC6804 와 SPI-A 경합 시 저장이 무음 실패하는 경로 |

> 마지막 두 항목은 원 보고서에 없던 것으로, 이번 코드 대조 과정에서 확인되었다.

---

## 7. 검증 계획

| # | 항목 | 확인 방법 | 기대 결과 |
|---|---|---|---|
| 1 | 빌드 | CCS 빌드 | 에러 · 경고 없음 |
| 2 | NVR 진단 | `NVRAllRegs.SRStatus` / `NvrOk` | SRStatus BP=00, NvrOk = 1 |
| 3 | N1 (OFF 10 s 이상) | 재기동 SOC | 40.0 % (NVR 채택) |
| 4 | N2 (OFF 3~5 s) | 재기동 SOC | 40.0 % |
| 5 | 버전 갱신 첫 부팅 | 재기동 SOC | OCV 초기화 1회 후 이후 정상 |
| 6 | LTC6804 통신 | 셀전압 측정 정상 여부 | NVRAM 접근 후에도 오류 없음 |
| 7 | F-6 완만 보정 | READY 30분 유지 후 SOC 추이 | 0.01 %p/s 로 OCV 방향 이동, 1회 5 %p 상한 |
| 8 | 전류한계 | CAN 0x604 연속 충전 한계 | R3 표 값과 일치 |
| 9 | UnbalPwr 차단 | 연속 한계 초과 상태 유지 | 알람 · 폴트 미발생, 0x604 값은 정상 |

---

## 8. 개정 이력

| Rev | 일자 | 내용 |
|---|---|---|
| R1 | 2026-08-31 | 최초 작성 — Ver 0.18 변경 내역 정리 |
