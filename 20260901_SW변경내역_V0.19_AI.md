# SW 변경내역 — F28069 Pack BMS (V0.19)

- **제품/시스템**: 현대 10kW 수소연료전지(FC) Pack BMS · F28069 (80V, 22S)
- **버전**: V0.18 → **V0.19** (Product_Type=0 시험 / Product_Version=19)
- **작성일**: 2026-09-01
- **작성자**: leewoowon (Note1)
- **기준 커밋**: 80fa763 (V0.18, 2026-09-01)
- **변경 근거 문서**: `10KWFCUBAS_PrtotectSet(260901)R9.md`
- **검증상태**: 검증중

---

## 1. 요약

보호설정표 **R8 → R9** 반영. 260830 실측에서 드러난 **경고·차단 임계 충돌**과 **셀 저온 경고 대소 역전 1건**을 해소했다.
`parameter.h` 설정값 8건만 변경했고 판정 로직은 손대지 않았다.
R9 지시 중 `C_PackVoltOV_Fault`(92.4) 1건은 **설계 판단으로 미반영**하고 91.3 을 유지했다(2-3 절).

R9 핵심은 「셀 기준값을 22S 환산해 팩 임계를 정하되, 경고와 차단이 같은 지점에 겹치지 않게 계층을 분리」하는 것이다.

---

## 2. 변경 상세 (parameter.h)

### 2-1. 경고 발생·해제값 — 6건

| No | 매크로 | 이전 | V0.19 | 근거 |
|---:|---|---:|---:|---|
| 5 | `C_PackVoltUN_Warn` | 66.0 | **65.45** | 셀 2.975 V × 22S. 종전 66.0 은 셀 3.000 V 환산이라 셀 저전압 경고와 동일 지점 |
| 5 | `C_PackVoltUN_WarnRst` | 69.3 | **68.7** | 히스테리시스 5 % (65.45 × 1.05 = 68.7225) → 소수 1자리 반올림 |
| 7 | `C_PackTempUN_Warn` | -25.0 | **-20.0** | 260901 `[원복]` 취소 |
| 7 | `C_PackTempUN_WarnRst` | -23.8 | **-19.0** | 〃 |
| 13 | `C_CellTempUN_Warn` | -20.0 | **-25.0** | 〃 |
| 13 | `C_CellTempUN_WarnRst` | -23.75 | **-23.5** | 표준율 5 % → **6 %** 적용 |

**원복 취소 사유** — 되돌린 상태(팩 -25 / 셀 -20)에서는 셀 저온 경고가
「발생 -20 이하 · 해제 -23.75 초과」로 **대소가 역전**된다.
예를 들어 -21 ℃ 이면 발생 조건과 해제 조건이 동시에 성립해 경고가 서자마자 풀린다.
팩·셀 임계를 다시 스왑해 이를 해소했다.

### 2-2. 보호(차단) 임계값 — 2건

| No | 매크로 | 이전 | V0.19 | 근거 |
|---:|---|---:|---:|---|
| 22 | `C_PackTempUN_Fault` | -25.0 | **-27.0** | 셀 저온 경고(-25)와 겹쳐 경고·차단 동시 발생 → 2 ℃ 분리 |
| 26 | `C_CellVoltDIV_Fault` | 0.35 | **0.40** | 셀 전압편차 차단 350 → 400 mV |

### 2-3. R9 지시 중 미반영 — 1건

| No | 매크로 | R9 지시 | V0.19 | 사유 |
|---:|---|---:|---:|---|
| 19 | `C_PackVoltOV_Fault` | 92.4 | **91.3 유지** | 설계 판단으로 종전 값 유지 |

> R9 §9.2 는 「91.3 은 셀 4.150 V 환산이라 셀 과전압 경고(4.15)와 같은 지점이므로
> 경고 없이 즉시 차단된다」는 이유로 92.4(셀 4.20 × 22S)를 지시했다.
> **91.3 을 유지하면 이 지적 사항이 그대로 남는다** — 팩 과전압 차단과 셀 과전압 경고가
> 동일 지점(4.150 V)에서 걸리므로, 셀 과전압 경고 bit8 과 팩 과전압 차단 bit19 가
> 사실상 동시에 SET 된다. 시험·보고 시 이 동작을 정상으로 간주할 것.

### 2-4. 버전

```c
//#define     Product_Version                    18   // (이전값 보존)
#define     Product_Version                      19   // TODO : [변경] 260901_Note1, 0.19 Product_Version 18->19 (보호설정표 R9 반영)
```

---

## 3. 보호 계층 검증 (R9 §9.1 대조)

| 물리량 | 계층 순서 (경고 → 차단) | 판정 |
|---|---|:--:|
| 저전압 | 셀 경고 3.000 → **팩 경고 2.975** → 셀 차단 2.650 = 팩 차단 2.650 | 일치 |
| 과전압 | 팩 경고 4.130 → **셀 경고 4.150 = 팩 차단 4.150** → 셀 차단 4.200 | **불일치** (2-3 절) |
| 저온 | 팩 경고 -20 → 셀 경고 -25 → **팩 차단 -27** → 셀 차단 -30 | 일치 |
| 고온 | 팩 경고 47 → 팩 차단 52 → 셀 경고 55 → 셀 차단 60 | 일치 |
| SOC | 경고 5 / 95 % → 차단 0 / 100 % | 일치 |
| 전류 | 경고 450 → 시간 480(10 s) → 반복 480(5회/min) → 차단 505 A | 일치 |
| 전압 편차 | 경고 200 → 차단 400 mV | 일치 |
| 온도 편차 | 경고 10 → 차단 20 ℃ | 일치 |

저전압의 셀·팩 차단이 같은 값(2.650 V)인 것은 **백업 계층**으로 의도한 것이다(어느 쪽이 먼저 검출되어도 동일 지점에서 차단).

과전압만 계층이 어긋난다. `C_PackVoltOV_Fault` 91.3(= 셀 4.150 × 22S)이 **셀 과전압 경고와 같은 지점**이라,
팩 전압이 91.3 V 에 닿는 순간 경고(bit8)와 차단(bit19)이 함께 선다. 92.4 미반영에 따른 결과이며 의도된 상태다.

---

## 4. 변경하지 않은 항목 (확인 결과)

| 구분 | 항목 | 사유 |
|---|---|---|
| 경고 Delay | 전 14항목 | R9 §3 「100 ms」와 현재 값이 이미 동일. No.8 만 10 s(`C_PackUnbalPwr_WarnDelay` 10000) 로 R9 표와 일치 |
| 차단 Delay | 전 21항목 | R9 「즉시 = 0」과 이미 일치 |
| No.34 `Bsa_FltOcTime_min` | 5 Count/min | `C_PackOCEventCount` 5 로 이미 반영(V0.16) |
| No.22 조건 「이상 → 이하」 | 판정식 | `Bat80VCellAgvTemperatureF <= C_PackTempUN_Fault` 로 이미 「이하」 |
| No.8 · 23 `UnbalPwr` | 경고·차단 판정 | **V0.17 에서 임시 차단 완료**. R9 「261220 이후 반영」과 일치하므로 추가 조치 없음 |
| No.30 · 32 · 36 | 릴레이 / 셀 내부저항 / 절연저항 | 261220 이후 반영. `TODOS : [2nd]` 구역으로 표시된 상태 유지 |
| No.35 `BPA_FltISOSPI_Err` | 210 CONT | 설정값만 정렬(V0.16), 판정 로직 미구현. R9 에서도 Release·Effect 공란 |

---

## 5. R9 문서 자체 오기 (설정값 반영 시 주의)

R9 본문 표 일부가 §1 변경표·§9 논리검증과 어긋난다. **§1·§9 를 정본으로 보고 반영**했다.

| 위치 | 표 기재 | 정본 (§1·§9.2·§9.4) | 반영값 |
|---|---:|---:|---:|
| §3 No.5 `Bsa_WrnUv` | 66 / 69.3 | 65.45 / 68.7225 | **65.45 / 68.7** |
| §4 No.19 `Bsa_FltOv` | 91.3 | 92.40 | **91.3 (미반영 결정, 2-3 절)** |
| §4 No.22 `Bsa_FltUt` | -25 | -27.0 | **-27.0** |

> 엑셀 갱신 시 §3·§4 표를 위 값으로 맞출 것.

---

## 6. 변경 파일

| 파일 | 변경 |
|---|---|
| `SysInclude/parameter.h` | 설정값 8건 + `Product_Version` 18 → 19 + **보호·경고 4개 섹션 주석 정리** |

- 활성 정의 중복 없음(변경 8건 + 버전 매크로 각 1개씩 확인).
- 판정 로직(`DSP28x_Project.c`) 변경 없음.

### 6-1. 주석 정리 (파일 630 → 468 줄)

누적된 비고 박스와 주석 처리된 옛 정의를 걷어내고, 각 매크로를 **한 줄 + 값 이력** 형태로 통일했다.
대상은 `//Fault Set Vaule` · `//Fault Delay Time` · `//Warning(Alarm) Set Vaule` · `//Warning(Alarm) Hold Time` 4개 섹션.

```c
// 정리 전 (13줄)
/*--------------------------------------------------------------
 * 260830 : 팩 저온 FAULT -35.0 → -25.0 도. ...
 *--------------------------------------------------------------*/
//#define     C_PackTempUN_Fault      -35.0   //26.05.30기준 -35도 (이전-30.0)
/*--------------------------------------------------------------
 * 260901 : 보호설정표 R9 — 팩 저온 차단 -25.0 → -27.0 도. ...
 *--------------------------------------------------------------*/
//#define     C_PackTempUN_Fault      -25.0   // TODO : [튜닝] 260830_Note1, 0.16 ...
#define     C_PackTempUN_Fault      -27.0   // TODO : [튜닝] 260901_Note1, 0.19 ...

// 정리 후 (1줄)
#define     C_PackTempUN_Fault      -27.0   // 팩 저온 차단 : -30.0->-35.0(초기)->-25.0(V0.16)->-27.0(V0.19)
```

| 표기 | 뜻 |
|---|---|
| `초기값` | 26.05.30 설정표 이후 변경 없음 |
| `A(초기)->B(V0.15)` | 초기 A 에서 V0.15 에 B 로 |
| `A->B(초기)` | 초기 설정표 안에서 이미 A→B (버전 이전) |
| `신규(V0.16)` | 해당 버전에 새로 만든 매크로 |

- 매크로 **이름·값 265개 전부 동일**함을 diff 로 검증했다(주석만 변경).
- 옛 주석의 서술형 사유는 버전별 SW 변경내역 문서(V0.12~V0.19)에 남아 있다.

---

## 7. 후속 조치

| # | 항목 | 담당 |
|---:|---|---|
| 1 | CCS 빌드 검증 | 사용자 |
| 2 | 재시험 4건 — No.5 · 13 · 22 · 26 (No.19 는 값 미변경) | 시험 |
| 3 | 시험 도구 온도 단하 속도 — 판정 임계 ±1 ℃ 구간 0.5 ℃/s (R9 §9.4 ⑤) | 시험 |
| 4 | R9 §3 · §4 표 오기 정정 후 엑셀 갱신 | 설계 |
| 5 | 보고서 `20260831_...보호검증시험보고서R8_AI.pptx` 재시험 결과 반영 후 R9 확정 | 설계 |
