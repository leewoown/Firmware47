# SW 변경내역 — F28069 Pack BMS (V0.16)

- **제품/시스템**: 현대 10kW 수소연료전지(FC) Pack BMS · F28069 (80V, 22S)
- **버전**: V0.15 → **V0.16** (Product_Type=0 시험 / Product_Version=16)
- **작성일**: 2026-09-01
- **작성자**: leewoowon (Note1)
- **기준 커밋**: d397f14 (V0.15, 2026-08-09)
- **검증상태**: 검증중

---

## 1. 요약
V0.15(R9 규격 정렬) 이후, **통신규약서 R4~R11** 및 **P56 안전 전류제한** 문서를 반영한 임계값·보호 로직 정비. 폴트 동작 검증을 위한 유지시간 단축, 반복 전류(OC event) 집계 파라미터 신설, SOC/알람/폴트 판정 개편 포함.

## 2. 변경 상세

### 2-1. 파라미터 (parameter.h)
| 매크로 | 값 | 근거 |
|--------|-----|------|
| Product_Version | 16 (VER 0.16) | 버전 확정 |
| Product_Voltage | 768 → **809** | R11 Normal_Volt 80.96V (22S×3.68) |
| Product_Capacity | 400 → **564** | R11 Capacity 56.4Ah |
| C_PackOCTimerCount | **10000** | 최대전류 10sec 유지 (480A, R6) |
| C_PackOCEventCount / Window | **5 / 60000** | 최대전류 반복 1분당 5회 집계 (R6, 신설) |
| C_ISOSPICount | **210** | ISOSPI 통신오류 카운트 (R4, 판정 로직 미구현) |
| C_PackVoltUN_Fault | **58.3** | 2.65V×22S (R4) |
| C_CellVoltUN_Fault | **2.65** | R4 |
| C_PackTempUN_Fault | **-25.0** | 셀 저온 폴트(-30) 보다 먼저 검출 (260830) |
| C_CellTempDIV_Fault | **20.0** | 셀온도편차 폴트 20도 확정 (경고 10도와 분리) |
| Warn 해제 정확값 (SOCOV/VoltOV/TempOV 등) | R6 정확값 | 90.86 / 88.1342 / 44.65 등 |
| **FaultDelay 대부분** | **0 (즉시)** | 폴트 **동작 검증용** (기존 2000/5000ms → 0) |
| C_PackUnbalPwr_FaultDelay | **15000** | 전류제한 초과 15sec 유지 (R6) |

### 2-2. 폴트/알람 판정 (DSP28x_Project.c)
- `Cal80VSysAlarmtCheck` / `Cal80VSysFaultCheck` **대폭 개편** — R9~R11 비트 정렬, 신규 임계값 적용.
- 최대전류 **반복 이벤트(OC event) 집계 로직** 추가 (1분 창 내 5회).
- `SysVarINIT` / `CANRegVarINIT` 초기화 변경.

### 2-3. SOC (BATAlgorithm.c)
- `CalSocRegsInit` / `CalSocHandle` 조정.

### 2-4. main.c / ISR
- `cpu_timer0_isr`, CAN 송신, 상태 보고 관련 정비.

### 2-5. 저온 경고 (parameter.h)
- WrnUt/WrnCellUt 저온 경고 임계값 정리 (세션 중 스왑 후 원복 — 최종 WrnUt −25 / WrnCellUt −20, WrnCellUt 해제 −23.75 R6).

## 3. 미반영 / 검증 대기 (TODO)
- **NVRAM 자체진단(NVR_Init/NVRAM_SelfTest, 260831)**: 세션 중 도입했다 **되돌림** — 현재 초기화 순서상 RAM함수(delay_us) 복사 전 호출로 **ILLEGAL_ISR(ITRAP) 크래시** 소지. 재도입 시 `MemCopy`/`InitFlash` 뒤로 배치 필요. 관련 `testTXbuf/testRXbuf` 크기(36) 확대도 함께 필요.
- **ISOSPI 통신오류(BPA_FltISOSPI_Err) 판정 로직 미구현** (카운트 매크로만 반영).
- **FaultDelay=0 은 동작 검증용** — 검증 완료 후 정상 유지시간 복원 필요.
- 상위 폴트비트 FAULT 기능, FltRly/FltIMD/FltCellIR 등 일부 항목 타버전 예정.

## 4. 변경 파일
`parameter.h`, `DSP28x_Project.c`, `DSP28x_Project.h`, `BATAlgorithm.c/.h`, `NVRAM.c/.h`, `BAT_LTC6802.c`, `ProtectRelay.c`, `SysVariable.h`, `main.c`

## 5. 참조 문서
통신규약서 CAN R9~R11, 10KWFCUBAS_PrtotectSet R3~R6, P56 Safety Current Limit R1~R3
