# SW 변경내역 — F28069 Pack BMS (V0.18)

- **제품/시스템**: 현대 10kW 수소연료전지(FC) Pack BMS · F28069 (80V, 22S)
- **버전**: V0.17 → **V0.18** (Product_Type=0 시험 / Product_Version=18)
- **작성일**: 2026-09-01
- **작성자**: leewoowon (Note1)
- **기준 커밋**: 40c937b (V0.17, 2026-09-01)
- **검증상태**: 검증중

---

## 1. 요약
**SPI-A 버스 공유(BATIC / NVRAM / (향후)MCP2515) 대응.** 각 디바이스가 **CS Low 직전에 자기 SPI 설정(극성·속도)을 직접 기입**하는 방식으로 통일하여, 다른 디바이스 접근으로 인한 속도 오염을 방지. (이전 "NVRAM 접근 후 SPIBRR=119 복원" 방식 대체)

## 2. 변경 상세

### 2-1. BAT_LTC6802.c — `BATSPIEnable_low()`
- CS Low 직전에 **BATIC SPI 설정 기입** 추가:
  - `CLKPOLARITY = 1` (Falling edge)
  - `SPIBRR = 60` → SPICLK = LSPCLK(20MHz)/(60+1) ≈ **328kHz** (현재 동작 확인된 값)

### 2-2. NVRAM.c — `NVR_SPIWrite()` / `NVR_SPIRead()`
- 기존 CS Low 직전 설정 기입 방식 유지, **주석 정리**:
  - `CLKPOLARITY = 1` (Falling edge — 기존 "Rising edge" 주석 오기 정정)
  - `SPIBRR = 50` → ≈ 392kHz (기존값 유지)
- 이전 버전의 "접근 후 SPIBRR=119 복원" 코드는 이 방식으로 대체(각자 CS-low 기입).

### 2-3. parameter.h
- Product_Version 17 → **18**.

## 3. 설계 근거
SPI-A를 BATIC(LTC6804)·NVRAM(MR25H40)이 공유. 한 디바이스가 SPIBRR을 바꾼 뒤 복원 안 하면 다른 디바이스가 잘못된 속도로 통신 → **각 디바이스의 CS Low 직전에 자기 설정을 매번 기입**하면 순서·상태 무관하게 항상 올바른 속도 보장.

## 4. 미반영 / 검증 대기 (TODO)
- BATIC `SPIBRR=60`(328kHz)은 "현재 동작 확인된 값" — 정식 검증 후 확정.
- (이전 이월) BATIC_Temp 값반영 소스 확정(BatICTempsF 라인), NVRAM 자체진단·ISOSPI 판정, FaultDelay=0 복원.

## 5. 변경 파일 (커밋 예정)
`BAT_LTC6802.c`, `NVRAM.c`, `parameter.h` (+ SW변경내역 md)

## 6. 참조
SPI-A 공유(BATIC/NVRAM/MCP2515), 통신규약 R13 기반
